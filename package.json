{
  "name": "gestion-facturas",
  "version": "1.0.0",
  "description": "Sistema de Gestión de Facturas",
  "main": "backend/server.js",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "npm install --prefix backend --production && node ./backend/server.js",
    "dev": "npm run dev --prefix backend",
    "test": "npm test --prefix backend"
  },
  "keywords": ["facturas", "invoices"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^15.8.1"
  }
}
